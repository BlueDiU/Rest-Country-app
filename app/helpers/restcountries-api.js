const NAME = 'https://restcountries.eu/',
  DOMAIN = `${NAME}rest/v2`,
  ALL = `${DOMAIN}/all`,
  BY_NAME = `${DOMAIN}/name/`,
  BY_REGION = `${DOMAIN}/region/`;

export default {
  NAME,
  DOMAIN,
  ALL,
  BY_NAME,
  BY_REGION,
};
